{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // 指定 TypeScript 编译器在编译过程中查找源文件的根目录为 "src" 文件夹
    "rootDir": "src",
    // 启用项目引用特性，允许 TypeScript 将此项目视为一个可组合的项目，
    // 这样可以实现增量编译，提高编译效率，常用于大型项目中多个子项目的管理
    "composite": true
  }
}
